# URL of your deployed backend (FastAPI/Railway service) — no trailing slash.
# Set this in Vercel / Netlify as an environment variable before deploying.
# Example: https://your-backend.up.railway.app
VITE_API_URL=
